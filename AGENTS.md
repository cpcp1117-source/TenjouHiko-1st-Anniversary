# Project Working Agreement

## Mission

逐步建立並驗證一個單一 HTML 的周年祝賀網站；每次只推進一個可驗證的 MVP 階段，不在尚未確認的視覺或互動上過度擴張。

## Language And Tone

- 專案文件與內容預設使用繁體中文。
- 程式欄位、驗證項目與交付結構可使用英文。
- 祝賀文字應溫柔、克制，保留使用者提供的真實互動細節與「妳」稱呼。

## Privacy Rules

- 不讀取、播放、轉碼、分析或上傳使用者的 WAV／MP3，除非使用者另行明確要求。
- 音訊在 MVP 0–2 保持本機且被 Git 忽略。
- 不把真實人物資料、私訊截圖或帳號資訊寫入 Repository。
- 不加入分析追蹤、第三方廣告或不必要的外部請求。

## Implementation Rules

- 正式入口為根目錄的 `index.html`。
- CSS 與 JavaScript 內嵌；圖片與音訊使用相對路徑。
- 使用語意化 HTML、鍵盤可操作控制項、可見焦點與替代文字。
- 尊重 `prefers-reduced-motion`。
- 音訊不得自動播放；同一時間只允許一首曲目播放。
- MP3 為可選優先來源，缺少時必須自動改用 WAV。
- 缺少全部音訊或圖片時，頁面不得崩潰。

## Phase Gate

每個 MVP 階段完成後，先留下驗證結果並等待使用者確認，再進入下一階段。規劃文件不是已完成的介面；靜態檢查也不能取代實際瀏覽器與手機尺寸驗證。

